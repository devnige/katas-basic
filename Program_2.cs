// using System;

// namespace basic_coding
// {
//     class Program_2
//     {
//         static void Main(string[] args)
//         {
            
            // 1. Write a program that prints ‘Hello World’ to the screen.
            // Console.WriteLine("Hello World!");

            // 2. Write a program that asks the user for their name and greets them with their name.
            // Console.WriteLine("Enter your name: ");
            // string personName = Console.ReadLine();
            // Console.WriteLine("Hello " + personName);
        
           
            
            // 3. Write a program that asks the user for their name
            // and only greets you if you are Alice or Bob.
            // Console.WriteLine("Enter your name: ");
            // string personName = Console.ReadLine();
            // if (personName == "Alice" || personName == "Bob")
            // {
            //     System.Console.WriteLine("Hello " + personName);
            // }

            // 4. Write a program that asks the user for a number n and prints the sum of the numbers 1 to n
            
            // int sum = 0;

            // Console.WriteLine("\n\n");
            // Console.WriteLine("A program to taka a number n and print the sum of numbers 1 to n");
            // Console.WriteLine("------------------------------------------------------------------");
            // Console.WriteLine("Enter a number: ");
            // var inputLastNum = Console.ReadLine();
            // var IsValidNum = Int32.TryParse(inputLastNum, out int lastNum);
            // if (IsValidNum) {
            //     Console.WriteLine("Your choice of number is {0}", lastNum);
            
            //     for (var i=1; i<=lastNum; i++)
            //             {
            //                 sum += i;
            //             }
            //     System.Console.WriteLine("The sum of all numbers from 1 to {0} is {1}", lastNum, sum);
            // }
            // else {
            //     System.Console.WriteLine("You entered {0}, please enter a valid number", inputLastNum);
            // }
        // ********************************************************

            // 5. Write a program that asks the user for a number n and prints the sum of the numbers 1 to n if the number is a multiple of three or five, e.g. 3, 5, 6, 9, 10, 12, 15 for n=17
           
            // int sum = 0;

            // System.Console.WriteLine(Environment.NewLine+Environment.NewLine);
            // Console.WriteLine("A program to take a number n and print the sum of numbers 1 to n if it is a multiple of three or five.");
            // Console.WriteLine("---------------------------------------------------------------------------------------------------------");
            // Console.WriteLine("Enter a number: ");
            // var inputLastNum = Console.ReadLine();
            // var IsValidNum = Int32.TryParse(inputLastNum, out int lastNum);
            // if (IsValidNum) {
            
            //     for (var i=1; i<=lastNum; i++) {
            //             if(i % 3 == 0 || i % 5 == 0)
            //                {
            //                 sum += i;
            //                 }
            //         }
                    
            // Console.WriteLine("The sum of all numbers from 1 to {0} that are divisible by 3 or 5 is {1} ", lastNum, sum);
            // }
            // else {
            //     System.Console.WriteLine("You entered {0}, please enter a valid number", inputLastNum);
            // }

//             6. Write a program that asks the user for a number n and gives them the possibility to choose between computing the sum and computing the product of 1,…,n.
//             int sum = 0;
//             int product = 0;

//             System.Console.WriteLine(Environment.NewLine+Environment.NewLine);
//             Console.WriteLine("A program to take a number n and print either the sum of numbers 1 to n or the product of the numbers 1 to n");
//             Console.WriteLine("---------------------------------------------------------------------------------------------------------");
//             Console.WriteLine("Enter a number: ");
//             var inputLastNum = Console.ReadLine();
//             var IsValidNum = Int32.TryParse(inputLastNum, out int lastNum);
//             if (IsValidNum) {
//                 // while != P or S do something
//                 Console.WriteLine("Enter S for Sum or P for Product: ");
//                 var inputChoice = Console.ReadLine();
//                 // toLower 
//                 // validate if it is s or p else ask user to enter a valid choice
//                 // if s do s routine, else do p routine
//                 if (inputChoice == "sum" || inputChoice = "product") { // should work but what about SUM, Sum and PRODUCT, Product
//                      if
                    
//              }
                    

//         }
//     }
// }
